import { SubtotalSumPipe } from './subtotal-sum.pipe';

describe('SubtotalSumPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtotalSumPipe();
    expect(pipe).toBeTruthy();
  });
});

import { NameCutterPipe } from './name-cutter.pipe';

describe('NameCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new NameCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
